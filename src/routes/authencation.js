// const express = require("express");
// const router = express.Router();
// const conexion = require("../config/conexion_BD");
// const passport = require("passport");

// const controller = require("../controllers/authController");

// /* router.post("/index", (req, res) => {
//   passport.authenticate("local.registro", {
//     successRedirect: "/index",
//     failureRedirect: "/registro",
//     failureFlash: true,
//   });
//   res.send("Recibido");
// }); */

// /* router.post(
//   "/registro",
//   passport.authenticate("local.registro", function (req, res, failure) {
//     if (failure) res.redirect("/registro");
//     res.redirect("/perfil" + req.cliContrasena.cliIdentificacion);
//   })
// );

// router.get("/perfil", (req, res) => {
//   res.send("Desde la ruta de perfil");
// }); */

// module.exports = router;
